apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-eks
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/jayaramrajagopal24/argocd-app.git
    targetRevision: HEAD
    path: dev
  destination: 
    server: https://kubernetes.default.svc
    namespace: argocd-eks

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
